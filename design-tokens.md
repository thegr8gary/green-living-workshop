# Design Tokens — 綠生活工作坊成果網站

本專案視覺規則的**單一真實來源**。每次新增頁面或修改樣式前先讀這份，不要憑記憶重新發想。
需要調整時回頭改這份，不要只改單一頁面的 code。

定案日期：2026-08-19

---

## 設計方向

沿用 `分組報告連結啟動頁/index.html` 的設計語言（老師指定：「Mia 設計的那個底還蠻好看的，照那個邏輯做」），但**修正其中不符無障礙標準的部分**。

## 色票

```css
:root{
  /* 綠 —— 深階當文字與標題底，亮階當裝飾 */
  --green-text:  #0b7a44;   /* 深綠：文字、標題列底色 */
  --green:       #0f9d58;   /* 亮綠：裝飾、邊框、色塊 */
  --green-tint:  #f2faf5;   /* 淡綠：區塊底色 */
  --green-on:    #e6f7ee;   /* 標題列上的副標文字 */

  /* 中性 */
  --ink:   #1a1a1a;         /* 內文 */
  --bg:    #ffffff;         /* 頁面底 */
  --muted: #5c6b62;         /* 次要文字 */
  --line:  #d8e5dc;         /* 分隔線 */
}
```

### 為什麼深綠是主力，不是亮綠

啟動頁用 `#0f9d58` 當主色沒問題——它只有 7 顆 `2.2rem` 的巨大按鈕，大字的 WCAG 門檻只要 3:1。

本站是**內容頁**，有大量一般大小的文字，門檻是 4.5:1。實算：

| 用法 | 對比 | 判定 |
|------|------|------|
| `#0f9d58` 內文 on 白 | 3.51:1 | ❌ 不到 4.5:1 |
| 白字 on `#0f9d58` | 3.51:1 | ❌ 不到 4.5:1 |
| `#cdeedd` on `#0f9d58`（啟動頁日期色） | 2.82:1 | ❌ 明顯不足 |
| **`#0b7a44` on 白** | **5.41:1** | ✅ |
| **白字 on `#0b7a44`** | **5.41:1** | ✅ |
| **`#e6f7ee` on `#0b7a44`** | **4.87:1** | ✅ |
| **`#1a1a1a` on 白** | **17.40:1** | ✅ |
| **`#0b7a44` on `#f2faf5`** | **5.09:1** | ✅ |
| `#5c6b62` on 白 | 5.96:1 | ✅ |

`#0b7a44` 本來就在啟動頁的 `:root` 裡（`--green-dark`），所以這**不是換色系**，是把同一套語言的深一階升為主力。

`#0f9d58` 仍可用於：邊框、圖示、色塊底、裝飾 blob（UI 元件邊界門檻是 3:1，3.51:1 足夠）。
**不可用於**：一般大小的文字。

### 不做深色模式

白底綠色，單一模式。不寫 `prefers-color-scheme` 分支。

---

## 字體

```css
font-family: -apple-system, "PingFang TC", "Microsoft JhengHei", sans-serif;
```

單一系統字家族，**不引 Google Fonts**。層級靠字重與字級拉開：

| 角色 | 字級 | 字重 |
|------|------|------|
| h1 標題 | `clamp(1.8rem, 3.6vw, 3rem)` | 800 |
| h2 副標 | `clamp(1rem, 1.6vw, 1.4rem)` | 500 |
| 區塊標題 | `clamp(1.3rem, 2.2vw, 1.8rem)` | 700 |
| 內文 | `1rem`（行高 1.85） | 400 |
| 註解／日期 | `0.9rem` | 400 |

`ponytail:` 刻意簡化——上限是「標題缺乏獨特字體個性」。真的需要時再引 Noto Serif TC 當 display 字體，屆時要接受 CDN 依賴或自行 self-host。目前優先保單檔無依賴。

---

## 間距

```css
--sp-1: .5rem;  --sp-2: 1rem;   --sp-3: 1.5rem;
--sp-4: 2.5rem; --sp-5: 4rem;   --sp-6: 6rem;
```

行高鬆（內文 1.85），區塊之間留白慷慨——空氣感優先。

---

## 簽名元素

**斜切綠色滿版標題列 + 地圖背景 + 模糊光暈**。這是全站唯一的視覺焦點，只出現在頁首。

```css
/* 桌機 */
clip-path: polygon(0 0, 100% 0, 100% 78%, 62% 78%, 55% 100%, 0 82%);
/* 手機 @media (max-width: 720px) */
clip-path: polygon(0 0, 100% 0, 100% 88%, 55% 88%, 45% 100%, 0 92%);
```

地圖背景：`url("assets/map-bw.png")`，**不 base64 內嵌**（啟動頁內嵌導致檔案膨脹到 1.35MB）。
光暈：`filter: blur(40px)` + `radial-gradient(circle, rgba(15,157,88,0.18), transparent 70%)`。

---

## 造型語彙（經三輪修正定案，不要重新發想）

**要**
- 斜切**只用在標題列**這種大面積切邊
- 保留塊狀結構——淡綠底色塊、卡片式分區要有，讓版面有骨架
- 空氣感：留白慷慨、行高鬆、字重不要過粗
- 形狀柔軟：圓角、柔光暈

**不要**
- 深色模式／黑底
- 方形圓角 + 加粗外框描邊的卡片（被指認為「像 AI 做的」）
- 銳利斜切用在小元件（節點、卡片、按鈕都不要）
- 塊狀太少、全靠留白分隔——會顯得空洞沒結構

修正歷程：v1 描邊卡片被指「像 AI 做的」→ v2 改斜切幾何被指「太銳利」→ v3 全拿掉改純留白被指「塊狀太少」。結論是**斜切標題列要留、塊狀要留、小元件柔軟、留白要夠**。

---

## 無障礙底線

啟動頁**沒有** focus 樣式、**沒有** reduced-motion 處理。這是缺陷，不是要沿用的風格。本站必須有：

```css
:focus-visible {
  outline: 2px solid var(--green-text);
  outline-offset: 3px;
}
```

所有動畫包在 `@media (prefers-reduced-motion: no-preference)` 裡——靜態是預設值，不是用 `reduce` 去關。

Accordion 用 `<button aria-expanded>` + `hidden`，狀態要同步（見 `~/.claude/skills/web-design/references/a11y-patterns.md` §2）。

---

## 技術約束

- **單一 HTML 檔**，CSS/JS 內嵌，無建置流程
- **無 CDN 依賴**——不引字體、不引 JS 庫、不用 GSAP
- 圖片放 `assets/`，用相對路徑引用
- 響應式斷點 `@media (max-width: 720px)`
- 中文為主；標題與各組題目中英並列，英文段落之後補
